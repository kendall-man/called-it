# Debug Journal — replay supervisor and escrow callback merge failures
Started: 2026-07-18T01:12:00+05:30
Goal: Fix merged engine failures around replay admission, supervisor completion/logging, and duplicate escrow callback durability.

## Environment snapshot
- Runtime: Node 26.5.0; pnpm 10.33.0; Vitest 3 via `@calledit/engine`.
- Git: `main` at `fe00d32`; large staged merge already present.
- References read: debugging `SKILL.md`, Node runtime, setup, investigation.

## Hypotheses
1. [OPEN] Callback/replay fakes implement an older supervisor contract and lack `admitReplayPosition`; direct type/runtime failures will identify the exact doubles. Fix: update fakes.
2. [OPEN] Supervisor completion/logging tests encode pre-merge completion behavior; observed assertion diffs will distinguish stale expectations from production regression. Fix: reconcile contract.
3. [OPEN] Duplicate signed escrow callbacks reach durable enqueue twice and the second `durable_enqueue_rejected` is treated as a hard placement failure instead of idempotent replay. Fix: classify duplicate.

## Artifacts to revert
- [ ] `.debug-journal.md` — remove after verification.

## Findings

## Final fix

