node_modules/
dist/
.next/
.turbo/
.env
.env.local
.env.*.local
*.tsbuildinfo
.DS_Store
coverage/
# NEVER commit TxLINE data or credentials (hackathon data license)
recordings/
*.jsonl.txline
txline-credentials.json
wallet.json

# personal tooling — not part of the project
.claude/

# Vercel CLI local project link
.vercel

# eve compiled artifacts
.eve
.output
.workflow-data

# Local agent tooling (generic skill packs, not project content)
.agents/

# Track only Called It project skills/workflows; keep generic installed packs ignored.
!.agents/
.agents/*
!.agents/skills/
.agents/skills/*
!.agents/skills/calledit-*/
!.agents/skills/calledit-*/**
!.agents/workflows/
.agents/workflows/*
!.agents/workflows/calledit-*.md

# Reproducible local runtime state and local Git bundles (never credentials in Git).
.calledit-local/
.calledit-backups/

# Local workflow/test artifacts; plans and evidence may contain runtime data.
.omo/
.tmp-telegram-evidence-test/
.superstack/
.surfpool/
playwright-report/
test-results/
